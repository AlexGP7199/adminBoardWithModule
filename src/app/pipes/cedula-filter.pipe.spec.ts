import { CedulaFilterPipe } from './cedula-filter.pipe';

describe('CedulaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CedulaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
