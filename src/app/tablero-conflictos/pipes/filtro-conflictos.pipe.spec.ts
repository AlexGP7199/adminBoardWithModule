import { FiltroConflictosPipe } from './filtro-conflictos.pipe';

describe('FiltroConflictosPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroConflictosPipe();
    expect(pipe).toBeTruthy();
  });
});
