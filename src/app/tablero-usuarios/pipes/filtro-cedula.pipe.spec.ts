import { FiltroCedulaPipe } from './filtro-cedula.pipe';

describe('FiltroCedulaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroCedulaPipe();
    expect(pipe).toBeTruthy();
  });
});
